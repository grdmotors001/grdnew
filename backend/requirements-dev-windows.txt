# Windows par local (SQLite) testing ke liye — psycopg2-binary aur
# gunicorn skip kiye hain, kyunki inko Windows par build karne ke liye
# PostgreSQL install karna padta hai jo local testing ke liye zaroori
# nahi hai. Deploy karte waqt Vercel (Linux) requirements.txt use karega,
# jisme dono cheezein hain.
Flask==3.0.3
Flask-SQLAlchemy==3.1.1
Flask-Cors==4.0.1
itsdangerous==2.2.0
python-dotenv==1.0.1
