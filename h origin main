warning: in the working copy of 'backend/app.py', LF will be replaced by CRLF the next time Git touches it
[1mdiff --git a/backend/app.py b/backend/app.py[m
[1mindex a939807..9bf3280 100644[m
[1m--- a/backend/app.py[m
[1m+++ b/backend/app.py[m
[36m@@ -58,11 +58,32 @@[m [mBASE_DIR = os.path.abspath(os.path.dirname(__file__))[m
 app = Flask(__name__)[m
 app.config["SECRET_KEY"] = os.environ.get("SECRET_KEY", "dev-secret-change-me")[m
 [m
[31m-db_url = os.environ.get("DATABASE_URL", f"sqlite:///{os.path.join(BASE_DIR, 'ebill.db')}")[m
[32m+[m[32mdb_url = os.environ.get([m
[32m+[m[32m    "DATABASE_URL",[m
[32m+[m[32m    f"sqlite:///{os.path.join(BASE_DIR, 'ebill.db')}"[m
[32m+[m[32m)[m
[32m+[m
[32m+[m[32m# Use the installed psycopg2-binary driver for PostgreSQL.[m
[32m+[m[32m# Some Vercel/Supabase DATABASE_URL values use the newer psycopg driver[m
[32m+[m[32m# prefix, but this backend is deployed with psycopg2-binary.[m
 if db_url.startswith("postgres://"):[m
[31m-    db_url = db_url.replace("postgres://", "postgresql+psycopg2://", 1)[m
[32m+[m[32m    db_url = db_url.replace([m
[32m+[m[32m        "postgres://",[m
[32m+[m[32m        "postgresql+psycopg2://",[m
[32m+[m[32m        1[m
[32m+[m[32m    )[m
[32m+[m
[32m+[m[32mif db_url.startswith("postgresql+psycopg://"):[m
[32m+[m[32m    db_url = db_url.replace([m
[32m+[m[32m        "postgresql+psycopg://",[m
[32m+[m[32m        "postgresql+psycopg2://",[m
[32m+[m[32m        1[m
[32m+[m[32m    )[m
[32m+[m
 if db_url.startswith("postgresql") and "supabase" in db_url and "sslmode=" not in db_url:[m
[31m-    db_url += ("&" if "?" in db_url else "?") + "sslmode=require"[m
[32m+[m[32m    db_url += ([m
[32m+[m[32m        "&" if "?" in db_url else "?"[m
[32m+[m[32m    ) + "sslmode=require"[m
 [m
 app.config["SQLALCHEMY_DATABASE_URI"] = db_url[m
 app.config["SQLALCHEMY_TRACK_MODIFICATIONS"] = False[m
